  　 <div class="chara-meta-top">
    　 <div class="chara-image">
      　 <%= attachment_image_tag @character, :image %>
    　 </div>
  　 </div>
    <div class="chara-content">
        <h2 class="chara-name"><%= @character.name %></h2>
        <ul class="meta-info-cells v4 float-wrapper">
          <li>
             <i class="fa fa-user"></i>By<a href="#">
             <%= @character.user.name %></a>
          </li>
          <li>
            <i class="fa fa-calendar"></i><%= character.created_at.strftime('%Y/%m/%d') %>
          </li>
          <% if @character.favorited_by?(current_user) %>
            <li>
          <%= link_to character_favorites_path(@character), method: :delete do %>
          <i class="fa fa-heart" aria-hidden="true" style="color: red;"></i>
      <%= @character.favorites.count %> いいね
    <% end %>
  </li>
<% else %>
  <li>
    <%= link_to character_favorites_path(@character), method: :post do %>
      <i class="fa fa-heart-o" aria-hidden="true"></i>
      <%= @character.favorites.count %> いいね
    <% end %>
  </li>
<% end %>
          <li>
            <i class="fa fa-comment"></i>
            <a href="#comments"><%= @character.comments.count %></a>
          </li>
          <li>
            <%= link_to "削除", post_image_path(@post_image), method: :delete %>
          </li>
        </ul>
        <div class="character-content-inner mt-20">
            <p><%= @character.introduction %></p>
        </div>
    </div>