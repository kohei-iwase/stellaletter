<%= form_for(@character) do |f| %>
 
    <% if @character.errors.any? %>
        <%= @character.errors.count %>件のエラーが発生しました
        <% @character.errors.full_messages.each do |message| %>
            <%= message %>
        <% end %>
    <% end %>
 
    <%= f.attachment_field :image %>
    <%= f.text_field :name %>
    <%= f.text_area :introduction %>
    <%= f.submit %>
<% end %>